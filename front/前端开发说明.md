# WEB新闻发布系统 - 前端开发说明

## 📋 前端任务分配

### 欧阳负责
- ✅ **Login.vue** - 用户登录页面（F6）
- ✅ **Register.vue** - 用户注册页面（F6）
- ✅ **api/auth.js** - 认证相关API

### 心怡负责
- ✅ **NewsList.vue** - 新闻列表页面（F1）
- ✅ **NewsDetail.vue** - 新闻详情页面（F3）
- ✅ **CreateNews.vue** - 发布新闻页面（F2）
- ✅ **api/news.js** - 新闻相关API（部分）

### 黄奕负责
- ✅ **CreateNews.vue** - 附件上传功能（F4）+ 指定接收人功能（F7）
- ✅ **NewsDetail.vue** - 下载附件功能（F5）
- ✅ **api/news.js** - 附件和接收人相关API

### 王世强/赵畅（共同）
- ✅ **Layout.vue** - 主布局组件
- ✅ **router/index.js** - 路由配置
- ✅ **utils/request.js** - HTTP请求封装
- ✅ **App.vue** - 根组件
- ✅ **main.js** - 入口文件

---

## 🚀 快速开始

### 1. 安装依赖

```bash
cd front
npm install
```

**如果安装慢，可以使用国内镜像：**
```bash
npm install --registry=https://registry.npmmirror.com
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问：http://localhost:3000

### 3. 构建生产版本

```bash
npm run build
```

---

## 📁 项目结构

```
front/
├── src/
│   ├── api/                    # API接口
│   │   ├── auth.js            # 认证API（欧阳）
│   │   └── news.js            # 新闻API（心怡、黄奕）
│   │
│   ├── components/             # 公共组件（可扩展）
│   │
│   ├── router/                 # 路由配置
│   │   └── index.js           # 路由定义（王世强/赵畅）
│   │
│   ├── utils/                  # 工具函数
│   │   └── request.js         # Axios封装（赵畅）
│   │
│   ├── views/                  # 页面组件
│   │   ├── Layout.vue         # 主布局（王世强/赵畅）
│   │   ├── Login.vue          # 登录页（欧阳）⭐
│   │   ├── Register.vue       # 注册页（欧阳）⭐
│   │   ├── NewsList.vue       # 新闻列表（心怡）⭐
│   │   ├── NewsDetail.vue     # 新闻详情（心怡+黄奕）⭐
│   │   └── CreateNews.vue     # 发布新闻（心怡+黄奕）⭐
│   │
│   ├── App.vue                 # 根组件
│   └── main.js                 # 入口文件
│
├── index.html                  # HTML模板
├── vite.config.js             # Vite配置
├── package.json               # 依赖配置
└── README.md                  # 项目说明
```

---

## 👥 个人开发指南

### 欧阳的任务（用户认证模块）

#### 需要完成的文件：
1. ✅ `src/views/Login.vue` - 已创建框架
2. ✅ `src/views/Register.vue` - 已创建框架
3. ✅ `src/api/auth.js` - 已创建框架

#### 开发步骤：
```bash
# 1. 确保后端API已启动
# 后端地址：https://localhost:5001

# 2. 启动前端项目
cd front
npm run dev

# 3. 测试登录注册功能
# 访问：http://localhost:3000/register
# 访问：http://localhost:3000/login
```

#### 测试checklist：
- [ ] 注册表单验证正常
- [ ] 注册成功后跳转到登录页
- [ ] 登录表单验证正常
- [ ] 登录成功后保存Token
- [ ] 登录成功后跳转到新闻列表

#### 可能遇到的问题：
**问题1：跨域错误**
```
Access to XMLHttpRequest has been blocked by CORS policy
```
**解决：** 后端需要配置CORS，找赵畅解决。

**问题2：网络请求失败**
```
Network Error
```
**解决：** 检查后端是否启动，检查URL是否正确。

---

### 心怡的任务（新闻核心模块）

#### 需要完成的文件：
1. ✅ `src/views/NewsList.vue` - 已创建框架（F1）
2. ✅ `src/views/NewsDetail.vue` - 已创建框架（F3）
3. ✅ `src/views/CreateNews.vue` - 已创建框架（F2）
4. ✅ `src/api/news.js` - 新闻相关API

#### 开发步骤：
```bash
# 1. 先用欧阳的登录功能登录
# 2. 访问新闻列表页面
# 访问：http://localhost:3000/news

# 3. 测试发布新闻
# 访问：http://localhost:3000/create

# 4. 测试查看详情
# 点击列表中的新闻
```

#### 测试checklist：
- [ ] 新闻列表正常显示
- [ ] 分页功能正常
- [ ] 点击行可以查看详情
- [ ] 发布新闻表单验证正常
- [ ] 发布成功后跳转到列表
- [ ] 新闻详情页显示完整内容

---

### 黄奕的任务（附件和接收人模块）

#### 需要完成的功能：
1. ✅ **F4：上传附件** - 在 `CreateNews.vue` 中
2. ✅ **F5：下载附件** - 在 `NewsDetail.vue` 中
3. ✅ **F7：指定接收人** - 在 `CreateNews.vue` 中
4. ✅ `src/api/news.js` - 附件和接收人API

#### 开发步骤：
```bash
# 1. 测试上传附件（F4）
# 在发布新闻页面选择文件上传

# 2. 测试下载附件（F5）
# 在新闻详情页点击下载按钮

# 3. 测试指定接收人（F7）
# 在发布新闻页面选择接收人
```

#### 测试checklist：
- [ ] 可以选择多个文件
- [ ] 文件上传成功
- [ ] 新闻详情显示附件列表
- [ ] 点击下载可以下载文件
- [ ] 可以打开接收人选择对话框
- [ ] 可以选择多个接收人
- [ ] 接收人显示正常

#### 注意事项：
- 文件大小限制：10MB
- 支持的文件格式：pdf, doc, docx, xls, xlsx, jpg, png, zip
- 下载文件时要处理文件流

---

## 🔧 技术要点

### 1. API请求封装

所有API请求都通过 `utils/request.js` 封装：

```javascript
import request from '@/utils/request'

// 示例：调用API
export function getNewsList(params) {
  return request({
    url: '/news',
    method: 'get',
    params
  })
}
```

**自动处理：**
- ✅ 自动添加Token到请求头
- ✅ 统一错误处理
- ✅ 响应数据提取

### 2. 路由守卫

在 `router/index.js` 中配置：

```javascript
router.beforeEach((to, from, next) => {
  if (to.meta.requireAuth) {
    const token = localStorage.getItem('token')
    if (!token) {
      next('/login')
    } else {
      next()
    }
  } else {
    next()
  }
})
```

**需要登录的页面自动跳转到登录页。**

### 3. Token管理

```javascript
// 保存Token
localStorage.setItem('token', res.token)
localStorage.setItem('userInfo', JSON.stringify(userInfo))

// 获取Token
const token = localStorage.getItem('token')

// 清除Token
localStorage.removeItem('token')
localStorage.removeItem('userInfo')
```

### 4. 文件上传

```javascript
// 使用 FormData
const formData = new FormData()
formData.append('file', file)

await uploadAttachment(newsId, file)
```

### 5. 文件下载

```javascript
const response = await downloadAttachment(attachmentId)

// 创建下载链接
const url = window.URL.createObjectURL(new Blob([response.data]))
const link = document.createElement('a')
link.href = url
link.setAttribute('download', fileName)
link.click()
```

---

## 🎨 UI组件库（Element Plus）

### 常用组件

```vue
<!-- 表单 -->
<el-form :model="form" :rules="rules">
  <el-form-item label="标题" prop="title">
    <el-input v-model="form.title" />
  </el-form-item>
</el-form>

<!-- 按钮 -->
<el-button type="primary" @click="handleClick">点击</el-button>

<!-- 表格 -->
<el-table :data="tableData">
  <el-table-column prop="name" label="姓名" />
</el-table>

<!-- 对话框 -->
<el-dialog v-model="visible" title="标题">
  内容
</el-dialog>

<!-- 消息提示 -->
import { ElMessage } from 'element-plus'
ElMessage.success('成功')
ElMessage.error('失败')

<!-- 加载中 -->
<div v-loading="loading">内容</div>
```

**完整文档：** https://element-plus.org/zh-CN/

---

## 🧪 测试流程

### 完整测试流程：

1. **启动后端**
   ```bash
   cd WEBNews.API
   dotnet run
   ```

2. **启动前端**
   ```bash
   cd front
   npm run dev
   ```

3. **测试顺序**
   ```
   注册 → 登录 → 新闻列表 → 发布新闻 → 上传附件 
   → 指定接收人 → 查看详情 → 下载附件
   ```

4. **记录测试结果**
   - 截图保存到 `front/screenshots/` 文件夹
   - 记录遇到的问题和解决方案

---

## 🐛 常见问题

### 问题1：npm install 失败

**症状：**
```
npm ERR! network timeout
```

**解决：**
```bash
# 使用国内镜像
npm config set registry https://registry.npmmirror.com
npm install
```

---

### 问题2：端口被占用

**症状：**
```
Port 3000 is in use
```

**解决：**
- 修改 `vite.config.js` 中的端口号
- 或关闭占用3000端口的程序

---

### 问题3：代理不工作

**症状：**
```
404 Not Found
```

**解决：**
检查 `vite.config.js` 中的代理配置：
```javascript
server: {
  proxy: {
    '/api': {
      target: 'https://localhost:5001',
      changeOrigin: true,
      secure: false
    }
  }
}
```

---

### 问题4：Element Plus 样式不显示

**解决：**
确保在 `main.js` 中导入了样式：
```javascript
import 'element-plus/dist/index.css'
```

---

## 📞 需要帮助？

### 前端问题
- 技术问题：@赵畅
- 协调问题：@王世强

### 后端API问题
- 用户认证：@欧阳
- 新闻模块：@心怡
- 附件模块：@黄奕

### 有用的命令

```bash
# 查看npm版本
npm -v

# 查看node版本
node -v

# 清除npm缓存
npm cache clean --force

# 重新安装依赖
rm -rf node_modules package-lock.json
npm install

# 查看运行的进程
lsof -i :3000
```

---

## ✅ 开发检查清单

### 欧阳（F6）
- [ ] 注册功能正常
- [ ] 登录功能正常
- [ ] Token保存正常
- [ ] 表单验证正常
- [ ] 错误提示正常

### 心怡（F1, F2, F3）
- [ ] 新闻列表显示正常
- [ ] 分页功能正常
- [ ] 发布新闻正常
- [ ] 新闻详情显示正常
- [ ] 日期格式化正常

### 黄奕（F4, F5, F7）
- [ ] 文件上传正常
- [ ] 文件下载正常
- [ ] 接收人选择正常
- [ ] 附件列表显示正常
- [ ] 接收人列表显示正常

---

**祝开发顺利！** 🎉

**文档维护人：王世强**  
**最后更新：2025-12-03**

